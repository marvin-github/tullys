<div style="background-color:lightgray">
<%= form_with(model: weight_category, local: true) do |form| %>
  <% if weight_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weight_category.errors.count, "error") %> prohibited this weight_category from being saved:</h2>

      <ul>
      <% weight_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container-fluid">
    <br>
    <h2 align="center"> Weight Category</h2>
    <br>
    <div class="row" >
      <div class="col-sm-3"></div>
      <div class="col-sm-6">
        <%= form.label :name %>
        <%= form.text_field :name, id: :weight_category_name, class: "form-control"  %>
      </div>
      <div class="col-sm-3"></div>
  </div><br>

  <div class="row" >
    <div class="col-sm-3"></div>
    <div class="col-sm-3">
      <%= form.label :description %>
      <%= form.text_field :description, id: :weight_category_description, class: "form-control"  %>
    </div>

    <div class="col-sm-3">
      <%= form.label :require_sale_weight %>
      <%= form.text_field :require_sale_weight, id: :weight_category_require_sale_weight, class: "form-control"  %>
    </div>
    <div class="col-sm-3"></div>
  </div><br>

  <div align="center">
    <div class="actions" >
      <%= form.submit :class => "btn btn-dark" %>
    </div>
  </div><br>
  </div>
  <% end %>
</div>